#pragma once
#include "SFML\Graphics.hpp"
using namespace sf;

static const Vector2f SIZE_BLOCK = { 100, 20 };

static const float UPPER_LIMIT = 100;

static const float LOWER_LIMIT = 600;

static const Vector2f START_POS = {700, 550};

static const float SPEED = float(0.09);